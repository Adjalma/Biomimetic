# GOOGLE APIS - Fase 6: Percepção Multimodal
# ===========================================
# Bibliotecas para integração com Google Calendar, Gmail, Drive APIs
# Instalação: pip install -r requirements/requirements_google.txt

google-api-python-client>=2.108.0
google-auth-httplib2>=0.1.0
google-auth-oauthlib>=1.0.0
google-auth>=2.23.0

# Utilitários para OAuth e HTTP
httplib2>=0.22.0
oauth2client>=4.1.3  # Para compatibilidade com alguns fluxos

# Notas:
# 1. Credenciais OAuth2 necessárias: credentials.json do Google Cloud Console
# 2. Primeira execução abrirá browser para autenticação
# 3. Token salvo em storage/token.pickle para reutilização
# 4. NUNCA commitar credentials.json ou token.pickle no Git!